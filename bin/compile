#!/bin/sh

indent() {
  sed -u 's/^/       /'
}

cd $1
git clone https://git.ffmpeg.org/ffmpeg.git ffmpeg
cd ffmpeg
CONFIG_RESULT=$(./configure | indent)
if [ $CONFIG_RESULT -ne 0 ]; then
    exit 1
fi
